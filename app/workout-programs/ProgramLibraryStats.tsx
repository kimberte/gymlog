"use client";

/**
 * Program usage counters are intentionally hidden site-wide.
 * Keep this component as a no-op so any legacy page importing it remains safe.
 */
export default function ProgramLibraryStats() {
  return null;
}
